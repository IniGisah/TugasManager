<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cooleyah.tugasmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TugasManager">
        <activity android:name=".CatatanAdd" />
        <activity android:name=".TugasAdd" />
        <activity android:name=".activity.About" />
        <activity android:name=".JadwalAdd" />
        <activity android:name=".activity.Stats" />
        <activity android:name=".activity.Timeline" />
        <activity android:name=".activity.More" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>